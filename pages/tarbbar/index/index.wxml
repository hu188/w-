<!--pages/tarbbar/index/index.wxml-->
<swiper class="screen-swiper square-dot" indicator-dots="true" circular="true" autoplay="true" interval="3000" duration="1000">
  <swiper-item wx:for="{{swiperList}}" wx:key="aa">
    <image src="{{item.url}}" mode="aspectFill" wx:if="{{item.type=='image'}}"></image>
    <video src="{{item.url}}" autoplay loop muted show-play-btn="{{false}}" controls="{{false}}" objectFit="cover" wx:if="{{item.type=='video'}}"></video>
  </swiper-item>
</swiper>
<view class="margin flex">
  <view class="module-box" bindtap="toExpress">
    <image src="/images/kd.png"  style="height:230rpx"></image>
    <view class="type-name">快递帮</view>
  </view>
  <view class="module-box" bindtap="toExpress">
    <image src="/images/pz.png"  style="height:230rpx"></image>
    <view class="type-name">拍一拍</view>
  </view>
</view>
<view class="margin flex">
   <view class="module-box" bindtap="toExpress">
    <image src="/images/kd.png"  style="height:230rpx"></image>
    <view class="type-name">快递帮</view>
  </view>
    <view class="module-box" bindtap="toExpress">
    <image src="/images/kd.png"  style="height:230rpx"></image>
    <view class="type-name">快递帮</view>
  </view>
</view>
